<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VinuezaValverdeVintimilla_P2.Views.NotePage"
             NavigationPage.HasNavigationBar="False">
    
    <VerticalStackLayout Spacing="10" Margin="5">

        <Grid ColumnDefinitions="*,*" ColumnSpacing="4">
            <Image Source="userlogo.png" MaximumHeightRequest="64"/>
            <Button Text="Postear" Grid.Column="1"
                    Clicked="SaveButton_Clicked" HeightRequest="40"
                    WidthRequest="150"/>
            <!--           
            <Button Grid.Column="1"
                    Text="Cancelar"
                    Clicked="DeleteButton_Clicked" />
            -->
        </Grid>
        <Editor x:Name="TextEditor"
                Margin="15"
                FontSize="20" 
                Placeholder="Cómo te sientes?"
                Text="{Binding Text}"
                HeightRequest="100" />
    </VerticalStackLayout>
</ContentPage>